
==============
How to install
==============

Here is the guide on how to install the *Imaging-Transcriptomics* tool so you can run it from your command line. 
The first step is to be sure to have :code:`python (v>=3.6)` and :code:`pip` installed.